# Local development only. Copy to .dev.vars; never commit secrets.
CLIENT_ID=
API_KEY=
MCP_ACCESS_TOKEN=

# R2_PUBLIC_BASE_URL is a non-secret Worker runtime variable.
# Configure its production value in Cloudflare Dashboard; it is intentionally not committed here.
