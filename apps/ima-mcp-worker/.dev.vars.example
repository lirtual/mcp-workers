# Local development only. Copy to .dev.vars; never commit secrets.
CLIENT_ID=
API_KEY=
MCP_ACCESS_TOKEN=

# Public HTTPS origin attached directly to the R2 bucket.
# Production uses https://ima-files.lirtual.dpdns.org
R2_PUBLIC_BASE_URL=https://ima-files.lirtual.dpdns.org
