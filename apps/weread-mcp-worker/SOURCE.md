# Source provenance

- Source repository: `lirtual/weread-mcp-worker`
- Frozen source commit: `8ab71db46b0298f3776a03fe48ffd49000477db4`
- Migration method: frozen snapshot copied into the monorepo; source history remains available in the referenced repository and commit.

## License status

The frozen source commit contains no root `LICENSE` file and no `license` field in `package.json`. Preserve that provenance fact and resolve licensing explicitly before any retirement/archive decision for the source repository.

The application has subsequently been adapted to the monorepo. Current runtime, ingress, authentication, and deployment policy are defined by the active README/configuration rather than this provenance record.
